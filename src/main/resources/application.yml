spring:
  application:
    name: productivity-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/productivity_db
    username: postgres
    password: 12345
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    open-in-view: false

  jackson:
    time-zone: America/Lima
    date-format: yyyy-MM-dd'T'HH:mm:ss
    default-property-inclusion: non_null

server:
  address: 0.0.0.0
  port: 8082
  error:
    include-message: always
    include-stacktrace: on_param

jwt:
  secret: tecsup_productivity_secret_key_2025_debe_ser_al_menos_256_bits_seguro
  expiration: 604800000

tecsup:
  api:
    url: https://tecsup.instructure.com/api/v1
    timeout: 30000

# ================================
# CONFIGURACIÓN DE GEMINI (MEJORADA)
# ================================
google:
  gemini:
    api:
      # ⚠️ IMPORTANTE: Mueve esto a variables de entorno en producción
      # Usa: export GEMINI_API_KEY=tu_key_aqui
      # Y reemplaza por: key: ${GEMINI_API_KEY}
      key: AIzaSyBlFuJAInS-UjrLotPZGYexjUNW1IA8tqY
      url: https://generativelanguage.googleapis.com/v1/models/gemini-2.5-flash:generateContent
      model: gemini-2.5-flash
      max-tokens: 1000
      temperature: 0.7

chatbot:
  context:
    max-tasks: 10
    max-events: 5
  session:
    timeout: 3600

# ================================
# LOGGING MEJORADO
# ================================
logging:
  level:
    root: INFO
    com.tecsup.productivity: DEBUG
    org.springframework.security: INFO
    org.springframework.web.reactive: INFO
    # Debug para diagnóstico de red (desactivar en producción)
    reactor.netty.http.client: DEBUG
    io.netty.resolver.dns: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

    # ================================
    # CONFIGURACIÓN DE PROXY (OPCIONAL)
    # Descomenta si TECSUP tiene proxy corporativo
    # ================================
    #spring:
    #  reactor:
    #    netty:
    #      http:
    #        client:
    #          proxy:
    #            type: HTTP
    #            host: proxy.tecsup.edu.pe
    #            port: 8080
    #            # Si requiere autenticación:
    #            username: tu_usuario
    #            password: tu_password









